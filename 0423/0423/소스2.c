//동적메모리#include <stdio.h>#include <malloc.h>void exam1();void exam2();void exam3();void exam4();int main(){	exam4();}//정수, 문자, 실수 하나씩 저장할 수 있는 동적 메모리 요구void exam1(){	int* p1 = malloc(sizeof(int));	char* p2 = malloc(sizeof(char));	float* p3 = malloc(sizeof(float));	//================================	*p1 = 10;	*p2 = 'a';	*p3 = 10.1f;	printf("%d, %c, %.1f\n", *p1, *p2, *p3);	//================================	free(p1);	free(p2);	free(p3);}//=================================================//전역변수/*int g_arr[5];void insert(){	int num = 10;	g_arr[0] = &num;}void exam2(){	//함수호출	insert();}*///힙메모리 활용1 : 지역변수의 주소를 저장하는 코드의 개선int* g_arr1[5];void insert11(){	int* pnum = (int*)malloc(sizeof(int));	*pnum = 10;	g_arr1[0] = pnum;}void insert12(){	int* pnum = (int*)malloc(sizeof(int));	*pnum = 20;	g_arr1[1] = pnum;}void exam3(){	insert11();	insert12();	printf("%d\n", *g_arr1[0]);	printf("%d\n", *g_arr1[1]);}
//활용예2 : 동적 배열
void exam4(){	//이 프로그램은 무조건 정수 10개만 저장할 수 있다. 	int arr1[10];	int size;	printf("정수 몇 개 저장 : ");	scanf_s("%d", &size);	int* arr = (int*)malloc(sizeof(int)* size);	//초기화	for (int i = 0; i < size; i++)	{		arr[i] = i;	}	//출력	for (int i = 0; i < size; i++)	{		printf("%d ", arr[i]);	}	printf("\n");	free(arr);}


